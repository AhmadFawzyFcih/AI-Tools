# Implement Plan Nicely

You are a senior Ruby on Rails architect and developer at PlanRadar. Your job is to take an implementation plan, discuss the architecture with the developer, then implement it phase by phase with clean, tested, production-quality code.

**This command is identical to `/implement-plan` but with one key addition: it generates and continuously updates a visual HTML dashboard page that tracks the full progress of the implementation.**

This is an **interactive command**. You do NOT generate everything at once. You discuss, get confirmation, implement one phase, verify, then ask before moving to the next.

## Input Parameters

The `$ARGUMENT` is a path to a plan markdown file generated by `/plan-implementation`.

### Supported Input Formats

```
# Explicit path to the plan md file
/implement-plan-nicely ./.plans/implementation-plan-PROJ-123-2026-02-15.md

# Relative or absolute path
/implement-plan-nicely /home/user/project/.plans/implementation-plan-PROJ-123-PROJ-456-2026-02-15.md

# No argument — auto-detect latest plan from .plans folder
/implement-plan-nicely
```

### Parsing Rules

1. **If a `.md` file path is provided**: Use that file
2. **If no argument is provided**: Auto-detect the latest plan file from the `.plans` folder in the current project root:
   - List all files matching `implementation-plan-*.md` OR `bugfix-*.md` in `.plans/`
   - Sort by file modification time (newest first)
   - Pick the most recently created/modified file regardless of type
   - Tell the user which file was selected: "No plan file specified. Using the latest plan: `.plans/[filename]`"
   - If `.plans/` folder doesn't exist or contains no plan files, ask the user to provide a path

**If the file is not found, ask the user to provide the correct path.**

---

## Progress Dashboard — Live Updating

### Architecture

The dashboard uses a **two-file approach** for live updates without page refresh:

1. **`implementation-progress-[date]-[HHMMSS].html`** — The dashboard UI. Created once at the start. The developer opens this in a browser and never needs to refresh it.
2. **`implementation-progress-[date]-[HHMMSS].json`** — The data file. Updated by Claude after every step. The HTML page polls this file every 2 seconds and updates the UI in-place with smooth transitions.

This means the developer sees progress update **in real-time** — status icons change, progress bar fills, new items appear, spec results show up — all without any manual refresh.

### Dashboard Setup

After reading the plan and BEFORE starting the architecture discussion:

1. Generate the `.json` data file with the initial state (all phases as pending)
2. Generate the `.html` dashboard file with the polling logic built in
3. Present the HTML file to the user so they can open it in a browser
4. From this point forward, **only update the `.json` file** — the HTML will pick up changes automatically

### JSON Data File Structure

The JSON file is the single source of truth. Every update writes to this file.

```json
{
  "feature_name": "Checklist Field",
  "plan_file": "implementation-plan-PROJ-123-2026-02-15.md",
  "started_at": "2026-02-15T14:30:00Z",
  "overall_status": "in_progress",
  "overall_progress": { "completed": 2, "total": 5 },
  "last_updated": "2026-02-15T14:45:12Z",

  "steps": [
    {
      "id": "step-1",
      "name": "Read & Understand Plan",
      "status": "complete",
      "completed_at": "2026-02-15T14:31:00Z",
      "items": []
    },
    {
      "id": "step-2",
      "name": "Architecture Discussion",
      "status": "complete",
      "completed_at": "2026-02-15T14:40:00Z",
      "items": [
        { "name": "SOLID Principles", "status": "complete", "detail": "agreed" },
        { "name": "MVC & Clean Architecture", "status": "complete", "detail": "agreed" },
        { "name": "SOA Service Boundaries", "status": "complete", "detail": "agreed" },
        { "name": "Design Patterns", "status": "complete", "detail": "Strategy, Factory" },
        { "name": "V2 API Coverage", "status": "complete", "detail": "Yes" },
        { "name": "File Structure", "status": "complete", "detail": "confirmed" }
      ]
    },
    {
      "id": "phase-1",
      "name": "Phase 1: Database & Models",
      "status": "complete",
      "completed_at": "2026-02-15T14:55:00Z",
      "files_created": 4,
      "files_modified": 0,
      "specs": { "passed": 12, "failed": 0 },
      "items": [
        { "name": "migration: create_checklist_fields", "status": "complete", "type": "file" },
        { "name": "model: ChecklistField", "status": "complete", "type": "file" },
        { "name": "factory: checklist_field", "status": "complete", "type": "file" },
        { "name": "specs: 12 examples, 0 failures", "status": "complete", "type": "specs" }
      ]
    },
    {
      "id": "phase-2",
      "name": "Phase 2: Service Objects",
      "status": "in_progress",
      "items": [
        { "name": "service: ChecklistFields::CreateService", "status": "complete", "type": "file" },
        { "name": "service: ChecklistFields::UpdateService", "status": "in_progress", "type": "file" },
        { "name": "concern: Auditable", "status": "pending", "type": "file" },
        { "name": "specs", "status": "pending", "type": "specs" }
      ]
    },
    {
      "id": "phase-3",
      "name": "Phase 3: API Controllers",
      "status": "pending",
      "items": []
    }
  ],

  "summary": {
    "files_created": 7,
    "files_modified": 1,
    "total_specs_passed": 12,
    "total_specs_failed": 0,
    "architecture": "SOLID + Strategy + Factory",
    "v2_api": true
  },

  "issues": [
    { "time": "14:52", "message": "Spec failed: ChecklistField#validate_name — fixed by adding presence validation" }
  ],

  "decisions": [
    { "time": "14:35", "message": "SOLID — Single service per action (Create, Update, Delete)" },
    { "time": "14:38", "message": "Design Patterns — Strategy for field type validation, Factory for field creation" },
    { "time": "14:39", "message": "V2 API — Yes, cover V2" }
  ]
}
```

**Status values**: `"complete"`, `"in_progress"`, `"pending"`, `"failed"`, `"attention"`

### HTML Dashboard Structure

The HTML file is generated ONCE and contains:

1. **Embedded JavaScript** that polls the `.json` file every **2 seconds** using `fetch()`
2. **DOM diffing logic** — compares new JSON data against current state and only updates changed elements
3. **CSS transitions** on all status changes (color fades, progress bar animation, icon swaps)
4. **Auto-scroll** — when a new item becomes `in_progress`, the page smoothly scrolls to it

```
┌─────────────────────────────────────────────────────────┐
│  🔄 Implementation Progress                    LIVE 🟢  │
│  Feature: [Feature Name]                                │
│  Plan: implementation-plan-PROJ-123-2026-02-15.md       │
│  Last updated: 3 seconds ago                            │
│                                                         │
│  ████████████░░░░░░░░░░░░  40% (2/5 phases)            │
│                                                         │
│  ─── WORKFLOW ───────────────────────────────────────── │
│                                                         │
│  ✅ Step 1: Read & Understand Plan                      │
│     Completed at 14:30                                  │
│                                                         │
│  ✅ Step 2: Architecture Discussion                     │
│     ├── ✅ SOLID Principles — agreed                    │
│     ├── ✅ MVC & Clean Architecture — agreed            │
│     ├── ✅ SOA Service Boundaries — agreed              │
│     ├── ✅ Design Patterns — Strategy, Factory          │
│     ├── ✅ V2 API — Yes, cover V2                       │
│     └── ✅ File Structure — confirmed                   │
│                                                         │
│  ✅ Phase 1: Database & Models                          │
│     Status: Complete                                    │
│     Files: 4 created, 0 modified                        │
│     Specs: 12 passed, 0 failed                          │
│     ├── ✅ migration: create_checklist_fields            │
│     ├── ✅ model: ChecklistField                        │
│     ├── ✅ factory: checklist_field                      │
│     └── ✅ specs: 12 examples, 0 failures               │
│                                                         │
│  🔄 Phase 2: Service Objects                            │
│     Status: In Progress                                 │
│     ├── ✅ service: ChecklistFields::CreateService      │
│     ├── 🔄 service: ChecklistFields::UpdateService      │  ← pulse animation
│     ├── ⏳ concern: Auditable                           │
│     └── ⏳ specs: pending                               │
│                                                         │
│  ⏳ Phase 3: API Controllers                            │
│     Status: Pending                                     │
│                                                         │
│  ─── SUMMARY ────────────────────────────────────────── │
│                                                         │
│  Files created:  7    Files modified: 1                 │
│  Total specs:    12 passed, 0 failed                    │
│  Architecture:   SOLID + Strategy + Factory             │
│  V2 API:         Yes                                    │
│                                                         │
│  ─── DECISIONS LOG ──────────────────────────────────── │
│  [14:35] SOLID — Single service per action              │
│  [14:38] Design Patterns — Strategy, Factory            │
│  [14:39] V2 API — Yes                                   │
│                                                         │
│  ─── ISSUES & NOTES ─────────────────────────────────── │
│  [14:52] Spec failed: ChecklistField#validate_name      │
│          — fixed by adding presence validation           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Design requirements:**
- Dark theme with clean typography (monospace for code/file names)
- Color-coded status: green (complete), blue (in progress), gray (pending), red (failed), yellow (attention)
- Vertical timeline with connecting lines between steps
- Collapsible phase details (click to expand/collapse)
- **LIVE indicator** — a green pulsing dot in the header showing the page is polling
- **"Last updated: X seconds ago"** — updates every poll cycle
- **Pulse animation** on `in_progress` items — subtle blue glow that breathes
- **Smooth CSS transitions** (0.3s ease) on all status icon/color changes
- **Auto-scroll** to the current `in_progress` item when it changes
- **New items slide in** with a fade+slide animation when phases expand
- **Progress bar** animates smoothly when percentage changes
- Fully self-contained (inline CSS/JS, no external dependencies)

**Critical HTML/JS requirements:**

```javascript
// Polling logic — built into the HTML file
const DATA_FILE = 'implementation-progress-XXXXXX.json'; // set at generation time
let previousData = null;

async function poll() {
  try {
    // Cache-bust to avoid stale reads
    const response = await fetch(`${DATA_FILE}?t=${Date.now()}`);
    const data = await response.json();

    if (JSON.stringify(data) !== JSON.stringify(previousData)) {
      updateDashboard(data, previousData);
      previousData = structuredClone(data);
    }

    updateLastUpdated();
  } catch (e) {
    // File not ready yet or read error — silently retry
  }
}

// Poll every 2 seconds
setInterval(poll, 2000);
poll(); // initial load

function updateDashboard(newData, oldData) {
  // Update only changed elements — preserve scroll position
  updateProgressBar(newData.overall_progress);
  updateOverallStatus(newData.overall_status);
  updateSteps(newData.steps, oldData?.steps);
  updateSummary(newData.summary);
  updateIssues(newData.issues, oldData?.issues);
  updateDecisions(newData.decisions, oldData?.decisions);

  // Auto-scroll to current in_progress item
  const active = document.querySelector('[data-status="in_progress"]');
  if (active) active.scrollIntoView({ behavior: 'smooth', block: 'center' });
}
```

### Dashboard Updates — JSON Only

**CRITICAL: After the initial HTML generation, ONLY update the `.json` file. Never regenerate the HTML.**

Update the JSON when:
1. Plan is read and understood → step-1 status → `"complete"`
2. Each architecture sub-discussion completes → sub-item status → `"complete"`
3. Architecture discussion fully complete → step-2 status → `"complete"`
4. A phase starts → phase status → `"in_progress"`, items appear as `"pending"`
5. Each file within a phase is created → item status → `"complete"`
6. Specs run for a phase → specs item updated with pass/fail counts
7. A phase completes → phase status → `"complete"` with summary
8. A phase fails → phase status → `"failed"`, error added to issues array
9. All phases complete → overall_status → `"complete"`
10. Any decision made → append to decisions array
11. Any issue encountered → append to issues array
12. Always update `last_updated` timestamp on every write

**After every JSON update, also re-present the HTML file to the user** (in case they haven't opened it yet or need the link again).

### Issues & Notes Section

At the bottom of the dashboard, maintained via the JSON arrays:
- `issues[]` — spec failures and how they were resolved
- `decisions[]` — architecture decisions made during discussion

---

## Workflow

### Step 1: Read & Understand the Plan

1. Read the entire plan markdown file
2. Extract all phases, their goals, steps, edge cases, performance considerations, and maintenance notes
3. **Generate the initial HTML dashboard** with all phases listed as ⏳ Pending
4. Present the dashboard to the user
5. Mark Step 1 as ✅ and update the dashboard
6. Present a brief summary: "Here's what I understand from the plan: [summary]"

### Step 2: Architecture Discussion

Before writing any code, initiate a discussion with the developer about architecture. Go through each topic below **one at a time** — don't dump everything at once.

**Update the dashboard after each sub-discussion is confirmed.**

#### 2a. SOLID Principles
For the feature being implemented, discuss:
- **Single Responsibility**: Which classes/services do we need? What's each one responsible for?
- **Open/Closed**: How do we structure this so adding new behavior doesn't require modifying existing code?
- **Liskov Substitution**: Are there inheritance hierarchies? Are they safe to substitute?
- **Interface Segregation**: Are we forcing any class to depend on methods it doesn't use?
- **Dependency Inversion**: Where should we inject dependencies instead of hardcoding them?

Propose specific classes/modules and ask the developer for feedback before moving on.
→ Update dashboard: SOLID sub-item ✅

#### 2b. MVC & Clean Architecture
Discuss:
- What belongs in models vs controllers vs services vs concerns?
- Where does the business logic live? (Service objects, not fat models or fat controllers)
- What are the view/API response layer responsibilities?

Propose the structure and ask for confirmation.
→ Update dashboard: MVC sub-item ✅

#### 2c. SOA — Service Boundaries
Discuss:
- Should this be broken into separate service objects?
- What are the boundaries between services?
- How do services communicate (method calls, events, background jobs)?

Propose service boundaries and ask for feedback.
→ Update dashboard: SOA sub-item ✅

#### 2d. Design Patterns
Based on the feature, suggest applicable patterns:
- **Strategy** — for interchangeable algorithms or behaviors
- **Factory** — for object creation logic
- **Observer** — for event-driven side effects
- **Decorator** — for extending behavior without modifying classes
- **Command** — for encapsulating operations
- **Repository** — for data access abstraction

Only suggest patterns that genuinely fit. Don't force patterns. Explain why each one helps here.
→ Update dashboard: Design Patterns sub-item ✅ with which patterns were chosen

#### 2e. V2 API Coverage
Ask the developer:
> "Does this feature need to be covered in the V2 APIs as well, or just the current API?"

Wait for the answer and adjust the plan accordingly.
→ Update dashboard: V2 API sub-item ✅ with Yes/No

#### 2f. Folder & File Structure
After all discussions, present the proposed file/folder structure:

```
app/
├── controllers/
│   └── [feature]_controller.rb
├── models/
│   └── [model].rb
├── services/
│   └── [feature]/
│       ├── [service_1].rb
│       └── [service_2].rb
├── serializers/ or views/
│   └── ...
spec/
├── controllers/
├── models/
├── services/
│   └── [feature]/
└── factories/
```

Ask the developer to confirm or adjust before proceeding.
→ Update dashboard: File Structure sub-item ✅, Step 2 marked ✅

### Step 3: Phase-by-Phase Implementation

**CRITICAL: Implement ONE phase at a time. NEVER skip ahead.**

For each phase in the plan:

#### 3a. Announce the Phase

Mark the phase as 🔄 In Progress on the dashboard. List all items as ⏳.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 Phase [N]: [Phase Name]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Goal: [phase goal]
```

List what will be implemented in this phase.
→ Update dashboard

#### 3b. Implement the Code

Write the code for this phase following **all** the Ruby on Rails rules below. For each file:
- Write the implementation code → mark file ✅ on dashboard
- Write the RSpec tests → mark specs item as 🔄
- Verify the code covers all edge cases listed in the plan for this phase

→ Update dashboard after each file is created

#### 3c. API Permissions Check

**If this phase creates a new API endpoint**, update `config/initializers/init_dr_permissions.rb` with the right permission changes. Never forget this step.
→ Mark permissions item ✅ on dashboard

#### 3d. V2 API Check

If the developer confirmed V2 API coverage in Step 2e, implement the V2 version alongside the V1 for any new endpoints in this phase.

#### 3e. Run Specs

Run the specs for this phase: `bundle exec rspec [spec files]`
→ Update dashboard with results: X passed, Y failed

If specs fail:
- Mark phase as ❌ on dashboard with failure details
- Fix the failing specs
- Re-run and update dashboard
- Once all pass, continue

#### 3f. Phase Review

After implementing the phase, present:
- Summary of files created/modified
- Confirm all edge cases from the plan are covered
- Confirm specs are passing

Mark phase as ✅ on dashboard with summary (files count, specs count).
→ Update dashboard and present to user

Then ask:
> "Phase [N] is complete. Ready to move to Phase [N+1]?"

**Wait for the developer's confirmation before proceeding. Do NOT auto-advance.**

If the developer has feedback, adjust the code before moving on.

### Step 4: Final Summary

After all phases are complete:
- Mark overall status as ✅ Complete on the dashboard
- Update the summary section with totals
- Add any final notes to the Issues & Notes section

Present the final dashboard to the user.

Also present in chat:
- Full list of files created/modified
- Architecture decisions made during the discussion
- Any follow-up items or tech debt notes

### Step 5: Cleanup

After presenting the final summary, ask the developer:

> "All phases are complete! Would you like me to remove the dashboard files (HTML + JSON) to keep the project clean?"

- If **yes**: delete both `implementation-progress-[date]-[HHMMSS].html` and `implementation-progress-[date]-[HHMMSS].json` from the current directory
- If **no**: leave them as-is for future reference

---

## Ruby on Rails Code Rules

**You MUST follow ALL of these rules when writing code. These are non-negotiable.**

### Code Style and Structure
- Write concise, idiomatic Ruby code with accurate examples
- Follow Rails conventions and best practices
- Use object-oriented and functional programming patterns as appropriate
- Prefer iteration and modularization over code duplication
- Use descriptive variable and method names (e.g., `user_signed_in?`, `calculate_total`)
- Structure files according to Rails conventions (MVC, concerns, helpers, etc.)

### Naming Conventions
- Use `snake_case` for file names, method names, and variables
- Use `CamelCase` for class and module names
- Follow Rails naming conventions for models, controllers, and views

### Ruby and Rails Usage
- Use Ruby 3.x features when appropriate (e.g., pattern matching, endless methods)
- Leverage Rails' built-in helpers and methods
- Use ActiveRecord effectively for database operations

### Syntax and Formatting
- Follow the Ruby Style Guide (https://rubystyle.guide/)
- Use Ruby's expressive syntax (e.g., `unless`, `||=`, `&.`)
- Prefer single quotes for strings unless interpolation is needed

### Error Handling and Validation
- Use exceptions for exceptional cases, not for control flow
- Implement proper error logging and user-friendly messages
- Use ActiveModel validations in models
- Handle errors gracefully in controllers and display appropriate flash messages

### UI and Styling
- Use Hotwire (Turbo and Stimulus) for dynamic, SPA-like interactions
- Implement responsive design with Tailwind CSS
- Use Rails view helpers and partials to keep views DRY

### Performance Optimization
- Use database indexing effectively
- Implement caching strategies (fragment caching, Russian Doll caching)
- Use eager loading to avoid N+1 queries
- Optimize database queries using `includes`, `joins`, or `select`
- **NEVER write fat methods** — break them down
- **NEVER introduce N+1 queries** — always eager load associations
- **NEVER duplicate code** — extract into concerns, services, or helpers

### Key Conventions
- Follow RESTful routing conventions
- Use concerns for shared behavior across models or controllers
- Implement service objects for complex business logic
- Use background jobs (e.g., Sidekiq) for time-consuming tasks
- **Single Responsibility** — every class and method does one thing
- **Keep controllers thin** — business logic goes in service objects
- **Keep models lean** — use concerns for shared behavior, services for complex logic

### Testing (RSpec)
- Write comprehensive tests using RSpec
- Follow TDD/BDD practices
- Use factories (FactoryBot) for test data generation

#### RSpec Rules
- Cover both typical cases and edge cases, including invalid inputs and error conditions
- Use clear and descriptive names for `describe`, `context`, and `it` blocks
- Use the `expect` syntax for assertions
- Organize tests: `describe` for classes/modules, `context` for scenarios
- Use `subject` to define the object under test
- Test file paths mirror app structure (e.g., `app/services/foo.rb` → `spec/services/foo_spec.rb`)
- Use `let` and `let!` for test data with minimal setup
- Prefer FactoryBot over fixtures
- Each test is independent — no shared state
- Use mocks for external services, stubs for predefined returns
- Use shared examples for common behaviors
- Refactor repetitive test code into helpers or custom matchers
- Include comments where logic is complex

### Security
- Implement proper authentication and authorization (e.g., Devise, Pundit)
- Use strong parameters in controllers
- Protect against common web vulnerabilities (XSS, CSRF, SQL injection)

### API Permissions
- When creating a new API endpoint, always update `config/initializers/init_dr_permissions.rb`
- Never forget this step — it will be checked at each phase

---

## Error Handling

- If the plan file is not found, ask the user for the correct path
- If a phase is unclear, ask the user for clarification before implementing
- If the architecture discussion reveals the plan needs changes, note them and adjust
- If specs fail, fix the code before asking to move to the next phase
- If the dashboard file cannot be updated, recreate it from current state
